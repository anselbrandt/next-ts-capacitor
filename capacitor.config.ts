import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'next-ts-capacitor',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
